version: '1.0'
steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: ncodefresh/yaml-node-3
    dockerfile: Dockerfile
    tag: latest

#  push:
#    type: push
#    candidate: ${{build-prj}}
#    tag: latest
#    credentials:
#      username: ${{username}}
#      password: ${{password}}


  unit-test:
    image: ${{build-prj}}
    commands:
      - pwd
      - ls -l
