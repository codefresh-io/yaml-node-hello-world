version: '1.0'

steps:
 test-comp:
   type: composition
   composition:
     db:
       image: mongo
     composition-candidates:
       web:
         image: 'node:latest'
         command: node --version
   composition-variables:
     - key=value