version: '1.0'
steps:
  build-prj:
    type: build
    description: e2e use case
    image-name: codefreshio/yaml-node-3
    dockerfile: Dockerfile
    tag: latest


  unit-test:
    image: noamt/pre-cached-sbt:latest
    working-directory: ${{initial-clone}}
    commands:
      - pwd
      - ls -l
